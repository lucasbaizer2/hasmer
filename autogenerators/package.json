{
  "name": "bytecode-format-generator",
  "version": "1.0.0",
  "main": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "compile": "tsc",
    "generate-bytecode-definitions": "yarn compile && node build/generate-bytecode-definitions.js",
    "compile-hermes-cli": "yarn compile && node build/compile-hermes-cli.js",
    "generate-instruction-documentation": "yarn compile && node build/generate-instruction-documentation.js"
  },
  "dependencies": {
    "fs-extra": "^10.0.1",
    "simple-git": "^3.1.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "@types/node": "^17.0.16",
    "typescript": "^4.5.5"
  }
}
